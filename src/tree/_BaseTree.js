// import { _setDefaults, _traverseTree } from './_utils';
class _BaseTree {
  constructor() {
  }
  predict(X) {
    // const root_node = this['root_node'];
    return X;
    // const prediction = _traverse_tree(root_node, example);
    // return prediction;
  }
}

export default _BaseTree;